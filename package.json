{
  "name": "byok-business-autopilot",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/templates",
    "packages/agents/extraction",
    "apps/router"
  ],
  "scripts": {
    "extract": "npm run extract --workspace @byok/extraction --",
    "router:test": "npm run test --workspace @byok/router"
  }
}

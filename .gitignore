# NEVER commit API keys or .env files — see docs/architecture/security-architecture.md

node_modules/
dist/
.output/
.vercel/
.env
.env.*
!.env.example
coverage/
.DS_Store

# LocalKms dev master key — see packages/vault/src/kms.ts. Never commit this.
.local-kms/

# TanStack Router auto-generates this from apps/web/src/routes/ on every
# dev/build run — never hand-edited, no reason to commit it.
routeTree.gen.ts
.tanstack/

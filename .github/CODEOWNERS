apps/router/ @veltrixnextgen-arch
packages/agents/ @veltrixnextgen-arch
packages/vault/ @veltrixnextgen-arch
packages/cost-gate/ @veltrixnextgen-arch
packages/approval-queue/ @veltrixnextgen-arch
docs/architecture/security-architecture.md @veltrixnextgen-arch

# packages/db is now load-bearing for trust-core: cost-gate's atomic
# reservation fix, approval-queue's durable state, and router's dedup all
# depend on its schema/RLS/withTenantScope for correctness, not just
# convenience. A change here can undermine the same guarantees a change to
# cost-gate or approval-queue directly would — same review bar (ADR-005's
# intent extended to the persistence layer trust-core now depends on).
packages/db/ @veltrixnextgen-arch

# R3/ADR-025: packages/jobs went from a generic, unused BullMQ wrapper to
# the thing that decides WHEN an unattended agent dispatch fires and
# enforces the cadence-floor cost-safety limits (docs/architecture/
# automation-runtime-plan.md §7) — the same "load-bearing for trust-core,
# same review bar" reasoning packages/db already carries above, extended
# to the scheduling layer trust-core now depends on.
packages/jobs/ @veltrixnextgen-arch

# ADR-009: the ESLint boundary rule and the CI gates that enforce it are
# part of the security posture, not shell code — a change here can
# silently disable the trust-core import boundary or the secret/audit
# gates, so it needs the same review as trust-core itself.
eslint.config.js @veltrixnextgen-arch
.github/workflows/ @veltrixnextgen-arch
.github/CODEOWNERS @veltrixnextgen-arch

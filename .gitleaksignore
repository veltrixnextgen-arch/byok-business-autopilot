# False positives from gitleaks' generic-api-key heuristic, verified by hand
# — each entry names the exact line and why it isn't a secret.

# docs/design/tool-registry.md line 54: plain prose describing gating
# processes ("...Google Business Profile's access-request form,
# Gusto/Rippling's partner pipeline)..."). No credential, key, or token of
# any kind appears on this line — confirmed by direct inspection of the
# flagged commit's diff.
d338bf33eb02fba4c2064950e83cfb400d89b21a:docs/design/tool-registry.md:generic-api-key:54

# packages/vault/src/vault.test.ts lines 166/182 (commit 272d4db): a
# hand-authored Hands-key test fixture whose placeholder value happened to
# match the stripe-access-token shape — never a real Stripe key. Renamed
# to a non-matching shape in the very next commit (0018e22), but gitleaks
# scans the full PR commit range, so this now-superseded commit's own
# diff still trips the rule — ignoring its fingerprint here rather than
# rewriting already-pushed history. (Do not quote the old placeholder
# value in this comment — gitleaks matches this file's own contents too.)
272d4dbbcc2201f1c5873f0e4b22d693c5228bec:packages/vault/src/vault.test.ts:stripe-access-token:166
272d4dbbcc2201f1c5873f0e4b22d693c5228bec:packages/vault/src/vault.test.ts:stripe-access-token:182
